# NeuConX — Python Dependencies
# SECURITY NOTE (Principal Architect):
# All versions are pinned to prevent supply chain attacks.
# Run `pip install -r requirements.txt --break-system-packages` on first launch.
# Regularly audit with `pip-audit` for known vulnerabilities.
# Never upgrade blindly — review changelogs for security patches first.

# ── Web Framework ──────────────────────────────────────────────────────────────
Flask==3.0.3
Werkzeug==3.0.3              # Flask dependency — pinned separately for security

# ── Security ───────────────────────────────────────────────────────────────────
Flask-Limiter==3.7.0         # Rate limiting — prevents DoS attacks
bleach==6.1.0                # HTML sanitization — prevents XSS
python-dotenv==1.0.1         # Secure env variable loading

# ── AI Model SDKs ──────────────────────────────────────────────────────────────
google-generativeai==0.8.3   # Gemini API
requests==2.32.3             # HTTP calls to NVIDIA, OpenRouter (pinned for CVE protection)
urllib3==2.2.2               # requests dependency — pinned (multiple past CVEs)

# ── Future Phases (uncomment when needed) ──────────────────────────────────────
# chromadb==0.5.20           # Phase 5: Vector database
# sentence-transformers==3.2.1  # Phase 5: Local embeddings
