# NeuConX — Dependencies (Phase 2–7)
# All versions pinned — supply chain security.
# Install: pip install -r requirements.txt --break-system-packages

# ── Web Framework ──────────────────────────────────────────────────────────────
Flask==3.0.3
Werkzeug==3.0.3

# ── Security ───────────────────────────────────────────────────────────────────
Flask-Limiter==3.7.0
bleach==6.1.0
python-dotenv==1.0.1

# ── AI SDKs & HTTP ─────────────────────────────────────────────────────────────
google-generativeai==0.8.3
requests==2.32.3
urllib3==2.2.2

# ── Phase 5: RAG / Semantic Memory ────────────────────────────────────────────
# Run: pip install chromadb sentence-transformers --break-system-packages
# First run downloads all-MiniLM-L6-v2 model (~80MB) — fully offline after that.
# App works fine without these — semantic search just stays disabled.
chromadb==0.6.3
sentence-transformers==3.2.1

# ── Feature: pdf_creator skill -> real PDF file generation ────────────────────
# reportlab: pure-Python, always works, no system dependencies. Required.
reportlab==4.2.5
# weasyprint: HTML/CSS -> PDF, nicer styling, used when available.
# Needs system libraries (Pango/Cairo/GDK-Pixbuf) — install separately if
# wanted; the app falls back to reportlab automatically if this is missing
# or fails at runtime. Safe to skip on Windows if those libs aren't installed.
#   pip install weasyprint --break-system-packages
# weasyprint==63.1