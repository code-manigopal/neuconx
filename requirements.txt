# NeuConX — Dependencies (Phase 2–7)
# All versions pinned — supply chain security.
# Install: pip install -r requirements.txt --break-system-packages

# ── Web Framework ──────────────────────────────────────────────────────────────
Flask==3.0.3
Werkzeug==3.0.3

# ── Security ───────────────────────────────────────────────────────────────────
Flask-Limiter==3.7.0
bleach==6.1.0
python-dotenv==1.0.1

# ── AI SDKs & HTTP ─────────────────────────────────────────────────────────────
google-generativeai==0.8.3
requests==2.32.3
urllib3==2.2.2

# ── Phase 5: RAG / Semantic Memory (optional but recommended) ──────────────────
# Uncomment and run: pip install chromadb sentence-transformers --break-system-packages
# chromadb==0.5.20
# sentence-transformers==3.2.1